# BorrowHood v1.0 Dependencies
# Pin versions for reproducible builds

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1

# Auth (JWT)
python-jose[cryptography]==3.3.0
httpx==0.28.1

# Validation & config
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Templates
jinja2==3.1.5

# PDF generation for Locandina (printable A6 event flyers, 4-up on A4 landscape).
# WeasyPrint renders Jinja-templated HTML to PDF server-side (no headless Chrome).
# Needs libpango / libpangocairo / libgdk-pixbuf / libffi-dev system libs -- see
# Dockerfile. qrcode produces the per-event QR PNG embedded in the card.
weasyprint==63.1
qrcode[pil]==8.0

# Utilities
python-slugify==8.0.4
python-multipart==0.0.20

# Testing
pytest==8.4.2
pytest-asyncio==0.24.0

# AI (Gemini)
google-genai>=1.65.0

# Telegram notifications
# (uses httpx -- already listed above)

# Object storage (MinIO)
minio==7.2.12

# Stripe payments (uses httpx for API calls -- no SDK needed)
lxml>=5.2.0
